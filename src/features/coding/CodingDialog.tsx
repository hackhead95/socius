// STUB — owned by the Text Coding agent. Rendered by the app shell's DialogHost for
// store dialogs of kind 'coding' (opened from the "Text coding" menu).
export function CodingDialog(props: { id: string; params?: Record<string, unknown>; onClose: () => void }) {
  return null;
}
